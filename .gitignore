# Personal config (contains user-specific coordinates)
Bot.ini

# Claude Code
.claude/

# Local AHK backup copies
Bot - *.ahk

# C++ bot runtime artifacts (when launched from repo root)
imgui.ini
poe-bot.log

